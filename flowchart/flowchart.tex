\documentclass[border = 1cm, a4paper]{standalone}

\usepackage{tikz}
\usetikzlibrary{shapes, arrows.meta, positioning, external}

\begin{document}

\begin{tikzpicture}

\node[draw,
    rounded rectangle,
    minimum width = 2.5cm,
    minimum height = 1cm] (start){START};

\node[draw,
    align=center,
    below=of start,
    minimum width=3.5cm,
    minimum height=1cm
] (initial) {Initialize Game (player, initial platforms)};

\node[draw,
    diamond,
    below=of initial,
    minimum width=2.5cm,
    inner sep=0] (mainloop) {Game ends?};

\node[draw,
    align = center,
    below left=of mainloop,
    minimum width=3.5cm,
    minimum height=1cm,
    inner sep=0] (get_time) {Get current time};
    
\node[draw,
    align = center,
    below = of get_time,
    minimum width=6.0cm,
    minimum height=2.0cm,
    inner sep=0] (player) {Move player\\ 
                        };
    
\node[draw,
    align = center,
    below = of player,
    minimum width=3.5cm,
    minimum height=1cm,
    inner sep=0] (platform) {Move platform};

\node[draw,
    align = center,
    below = of platform,
    minimum width=5.0cm,
    minimum height=2cm,
    inner sep=0] (hurtR) {Hurt recover: \\
                        recover from hurting \\
                        status after 3 seconds};

\node[draw,
    align = center,
    below = of hurtR,
    minimum width=3.5cm,
    minimum height=1cm,
    inner sep=0] (UsPB) {Unstable platform break};
    
\node[draw,
    align = center,
    below = of UsPB,
    minimum width=3.5cm,
    minimum height=1cm,
    inner sep=0] (nap) {Refresh \& nap};

\node[draw,
    diamond,
    below right=0.7 cm and 5cm of mainloop,
    minimum width=2.5cm,
    inner sep=0] (check) {Win?};

\node[draw,
    align = center,
    below right = of check,
    minimum width=3.5cm,
    minimum height=1cm,
    inner sep=0] (win) {Win procedure};

\node[draw,
    align = center,
    below left = of check,
    minimum width=3.5cm,
    minimum height=1cm,
    inner sep=0] (lose) {Lose procedure};

\draw[-latex] (start) edge (initial)
    (initial) edge (mainloop)
    (get_time) edge (player)
    (player) edge (platform)
    (platform) edge (hurtR)
    (hurtR) edge (UsPB)
    (UsPB) edge (nap);
\draw[-latex] (check) -| (win)
	node[pos=0.25,fill=white,inner sep=0.2cm]{Yes};
\draw[-latex] (check) -| (lose)
	node[pos=0.25,fill=white,inner sep=0.2cm]{No};
\draw[-latex] (mainloop) -| (check)
    node[pos=0.25,fill=white,inner sep=0.2cm]{Yes};
\draw[-latex] (mainloop) -| (get_time)
    node[pos=0.25,fill=white,inner sep=0.2cm]{No};
\draw[-latex] (nap) -| (mainloop);
 
\end{tikzpicture}


\end{document}
